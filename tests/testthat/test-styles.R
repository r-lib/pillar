# keep_empty --------------------------------------------------------------------------

test_that("`keep_empty()` snapshot test", {
  expect_snapshot({
    keep_empty()
  })
})


# style_subtle_num --------------------------------------------------------------------

test_that("`style_subtle_num()` snapshot test", {
  expect_snapshot({
    style_subtle_num()
  })
})


# style_spark_na ----------------------------------------------------------------------

test_that("`style_spark_na()` snapshot test", {
  expect_snapshot({
    style_spark_na()
  })
})


# style_na ----------------------------------------------------------------------------

test_that("`style_na()` snapshot test", {
  expect_snapshot({
    style_na()
  })
})


# pillar_na ---------------------------------------------------------------------------

test_that("`pillar_na()` snapshot test", {
  expect_snapshot({
    pillar_na()
  })
})


# style_list --------------------------------------------------------------------------

test_that("`style_list()` snapshot test", {
  expect_snapshot({
    style_list()
  })
})


# has_color ---------------------------------------------------------------------------

test_that("`has_color()` snapshot test", {
  expect_snapshot({
    has_color()
  })
})


# make_style_fast ---------------------------------------------------------------------

test_that("`make_style_fast()` snapshot test", {
  expect_snapshot({
    make_style_fast()
  })
})


# crayon_underline --------------------------------------------------------------------

test_that("`crayon_underline()` snapshot test", {
  expect_snapshot({
    crayon_underline()
  })
})


# crayon_italic -----------------------------------------------------------------------

test_that("`crayon_italic()` snapshot test", {
  expect_snapshot({
    crayon_italic()
  })
})


# crayon_red --------------------------------------------------------------------------

test_that("`crayon_red()` snapshot test", {
  expect_snapshot({
    crayon_red()
  })
})


# crayon_yellow -----------------------------------------------------------------------

test_that("`crayon_yellow()` snapshot test", {
  expect_snapshot({
    crayon_yellow()
  })
})


# crayon_bold -------------------------------------------------------------------------

test_that("`crayon_bold()` snapshot test", {
  expect_snapshot({
    crayon_bold()
  })
})


# crayon_grey_0.6 ---------------------------------------------------------------------

test_that("`crayon_grey_0.6()` snapshot test", {
  expect_snapshot({
    crayon_grey_0()
  })
})


# crayon_grey_0.8 ---------------------------------------------------------------------

test_that("`crayon_grey_0.8()` snapshot test", {
  expect_snapshot({
    crayon_grey_0()
  })
})


# assign_crayon_styles ----------------------------------------------------------------

test_that("`assign_crayon_styles()` snapshot test", {
  expect_snapshot({
    assign_crayon_styles()
  })
})
