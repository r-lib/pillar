# map ---------------------------------------------------------------------------------

test_that("`map()` snapshot test", {
  expect_snapshot({
    map()
  })
})


# map_mold ----------------------------------------------------------------------------

test_that("`map_mold()` snapshot test", {
  expect_snapshot({
    map_mold()
  })
})


# map_lgl -----------------------------------------------------------------------------

test_that("`map_lgl()` snapshot test", {
  expect_snapshot({
    map_lgl()
  })
})


# map_int -----------------------------------------------------------------------------

test_that("`map_int()` snapshot test", {
  expect_snapshot({
    map_int()
  })
})


# map_dbl -----------------------------------------------------------------------------

test_that("`map_dbl()` snapshot test", {
  expect_snapshot({
    map_dbl()
  })
})


# map_chr -----------------------------------------------------------------------------

test_that("`map_chr()` snapshot test", {
  expect_snapshot({
    map_chr()
  })
})


# map_cpl -----------------------------------------------------------------------------

test_that("`map_cpl()` snapshot test", {
  expect_snapshot({
    map_cpl()
  })
})


# walk --------------------------------------------------------------------------------

test_that("`walk()` snapshot test", {
  expect_snapshot({
    walk()
  })
})


# pluck -------------------------------------------------------------------------------

test_that("`pluck()` snapshot test", {
  expect_snapshot({
    pluck()
  })
})


# pluck_lgl ---------------------------------------------------------------------------

test_that("`pluck_lgl()` snapshot test", {
  expect_snapshot({
    pluck_lgl()
  })
})


# pluck_int ---------------------------------------------------------------------------

test_that("`pluck_int()` snapshot test", {
  expect_snapshot({
    pluck_int()
  })
})


# pluck_dbl ---------------------------------------------------------------------------

test_that("`pluck_dbl()` snapshot test", {
  expect_snapshot({
    pluck_dbl()
  })
})


# pluck_chr ---------------------------------------------------------------------------

test_that("`pluck_chr()` snapshot test", {
  expect_snapshot({
    pluck_chr()
  })
})


# pluck_cpl ---------------------------------------------------------------------------

test_that("`pluck_cpl()` snapshot test", {
  expect_snapshot({
    pluck_cpl()
  })
})


# map2 --------------------------------------------------------------------------------

test_that("`map2()` snapshot test", {
  expect_snapshot({
    map2()
  })
})


# map2_lgl ----------------------------------------------------------------------------

test_that("`map2_lgl()` snapshot test", {
  expect_snapshot({
    map2_lgl()
  })
})


# map2_int ----------------------------------------------------------------------------

test_that("`map2_int()` snapshot test", {
  expect_snapshot({
    map2_int()
  })
})


# map2_dbl ----------------------------------------------------------------------------

test_that("`map2_dbl()` snapshot test", {
  expect_snapshot({
    map2_dbl()
  })
})


# map2_chr ----------------------------------------------------------------------------

test_that("`map2_chr()` snapshot test", {
  expect_snapshot({
    map2_chr()
  })
})


# map2_cpl ----------------------------------------------------------------------------

test_that("`map2_cpl()` snapshot test", {
  expect_snapshot({
    map2_cpl()
  })
})


# args_recycle ------------------------------------------------------------------------

test_that("`args_recycle()` snapshot test", {
  expect_snapshot({
    args_recycle()
  })
})


# pmap --------------------------------------------------------------------------------

test_that("`pmap()` snapshot test", {
  expect_snapshot({
    pmap()
  })
})


# probe -------------------------------------------------------------------------------

test_that("`probe()` snapshot test", {
  expect_snapshot({
    probe()
  })
})


# keep --------------------------------------------------------------------------------

test_that("`keep()` snapshot test", {
  expect_snapshot({
    keep()
  })
})


# discard -----------------------------------------------------------------------------

test_that("`discard()` snapshot test", {
  expect_snapshot({
    discard()
  })
})


# map_if ------------------------------------------------------------------------------

test_that("`map_if()` snapshot test", {
  expect_snapshot({
    map_if()
  })
})


# compact -----------------------------------------------------------------------------

test_that("`compact()` snapshot test", {
  expect_snapshot({
    compact()
  })
})


# transpose ---------------------------------------------------------------------------

test_that("`transpose()` snapshot test", {
  expect_snapshot({
    transpose()
  })
})


# every -------------------------------------------------------------------------------

test_that("`every()` snapshot test", {
  expect_snapshot({
    every()
  })
})


# some --------------------------------------------------------------------------------

test_that("`some()` snapshot test", {
  expect_snapshot({
    some()
  })
})


# negate ------------------------------------------------------------------------------

test_that("`negate()` snapshot test", {
  expect_snapshot({
    negate()
  })
})


# reduce ------------------------------------------------------------------------------

test_that("`reduce()` snapshot test", {
  expect_snapshot({
    reduce()
  })
})


# reduce_right ------------------------------------------------------------------------

test_that("`reduce_right()` snapshot test", {
  expect_snapshot({
    reduce_right()
  })
})


# accumulate --------------------------------------------------------------------------

test_that("`accumulate()` snapshot test", {
  expect_snapshot({
    accumulate()
  })
})


# accumulate_right --------------------------------------------------------------------

test_that("`accumulate_right()` snapshot test", {
  expect_snapshot({
    accumulate_right()
  })
})


# detect ------------------------------------------------------------------------------

test_that("`detect()` snapshot test", {
  expect_snapshot({
    detect()
  })
})


# detect_index ------------------------------------------------------------------------

test_that("`detect_index()` snapshot test", {
  expect_snapshot({
    detect_index()
  })
})


# index -------------------------------------------------------------------------------

test_that("`index()` snapshot test", {
  expect_snapshot({
    index()
  })
})


# imap --------------------------------------------------------------------------------

test_that("`imap()` snapshot test", {
  expect_snapshot({
    imap()
  })
})


# vec_index ---------------------------------------------------------------------------

test_that("`vec_index()` snapshot test", {
  expect_snapshot({
    vec_index()
  })
})
