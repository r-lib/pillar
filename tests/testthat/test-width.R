# get_width ---------------------------------------------------------------------------

test_that("`get_width()` snapshot test", {
  expect_snapshot({
    get_width()
  })
})


# set_width ---------------------------------------------------------------------------

test_that("`set_width()` snapshot test", {
  expect_snapshot({
    set_width()
  })
})


# get_widths --------------------------------------------------------------------------

test_that("`get_widths()` snapshot test", {
  expect_snapshot({
    get_widths()
  })
})


# get_min_width -----------------------------------------------------------------------

test_that("`get_min_width()` snapshot test", {
  expect_snapshot({
    get_min_width()
  })
})


# set_min_width -----------------------------------------------------------------------

test_that("`set_min_width()` snapshot test", {
  expect_snapshot({
    set_min_width()
  })
})


# get_min_widths ----------------------------------------------------------------------

test_that("`get_min_widths()` snapshot test", {
  expect_snapshot({
    get_min_widths()
  })
})
