# pillar_shaft ------------------------------------------------------------------------

test_that("`pillar_shaft()` snapshot test", {
  expect_snapshot({
    pillar_shaft()
  })
})


# type_sum ----------------------------------------------------------------------------

test_that("`type_sum()` snapshot test", {
  expect_snapshot({
    type_sum()
  })
})
