# pillar_shaft.vctrs_unspecified ------------------------------------------------------

test_that("`pillar_shaft.vctrs_unspecified()` snapshot test", {
  expect_snapshot({
    pillar_shaft()
  })
})


# pillar_shaft.vctrs_vctr -------------------------------------------------------------

test_that("`pillar_shaft.vctrs_vctr()` snapshot test", {
  expect_snapshot({
    pillar_shaft()
  })
})


# type_sum.vctrs_vctr -----------------------------------------------------------------

test_that("`type_sum.vctrs_vctr()` snapshot test", {
  expect_snapshot({
    type_sum()
  })
})


# pillar_shaft.vctrs_list_of ----------------------------------------------------------

test_that("`pillar_shaft.vctrs_list_of()` snapshot test", {
  expect_snapshot({
    pillar_shaft()
  })
})
